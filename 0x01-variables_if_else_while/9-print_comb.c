#include <stdio.h>
/**
 * main - represent Entry poin
 * Return: 0 for sucess
 */
int main(void)
{
	int x;

	for (x = 0; x < 10; i++)
	{
	putchar(x + '0')
	if (x < 9)
	{
	putchar(',');
	putchar(' ');
	}
	}
	putchar('\n');
	return (0);
}
