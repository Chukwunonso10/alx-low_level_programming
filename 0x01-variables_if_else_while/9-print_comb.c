#include <stdio.h>
/**
 * main - Entry point of the program
 * Return: 0 for sucess
 */
int main(void)
{
	int x = 0;

	for (x = 0; x < 10; x++)
	{
	putchar(x + '0');
	if (x < 9)
	}
	{
	putchar(' ');
	putchar(',');
	}
	putchar('\n');
	Return (0);
}
