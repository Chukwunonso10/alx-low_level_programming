#include <stdio.h>
/**
 * main - Entry point into the programe
 * Return: 0 for sucess
 */
int main(void)
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
	{
	putchar(a);
	}
	putchar('\n');
	return (0);
}
		
