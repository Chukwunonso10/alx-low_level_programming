#include "main.h"
/**
 * main - Entry point of the programm
 * Return: 0 for sucess
 */
int main(void)
{
	int char;

	putchar(_);
	putchar(p);
	putchar(u);
	putchar(t);
	putchar(c);
	putchar(h);
	putchar(a);
	putchar(r);

	return (0);
}
